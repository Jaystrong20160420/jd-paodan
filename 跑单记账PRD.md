# 跑单记账 产品需求文档（PRD）

| 项目 | 内容 |
|---|---|
| 产品名称 | 跑单记账 |
| 文档版本 | V1.0（对应应用版本 V23） |
| 更新日期 | 2026-09-19 |
| 产品状态 | 已上线，持续迭代 |
| 访问地址 | https://9ut5efn5.qwenwork.host/ |
| 目标用户 | 外卖众包骑手（单用户自用） |

---

## 1. 产品概述

### 1.1 背景

用户从事京东众包外卖配送，收入按单结算、日结进入平台钱包。用户存在两类核心资金压力：

1. **固定还款压力**：多笔贷款/账单（如招联金融、信用卡预借现金）有固定扣款日，逾期有征信风险
2. **收入不确定**：每日跑单收入不固定，需要随时知道"还差多少钱、每天要跑多少单才能按时结清"

现有记账工具（如随手记、鲨鱼记账）面向多场景记账，无法针对"账单倒计时 + 日均跑单缺口"做深度定制；Excel 操作繁琐且手机端体验差。因此自建一个专注此场景的轻量应用。

### 1.2 产品定位

一个**跑单收入记录 + 账单倒计时 + 结清缺口测算**的单页应用，手机与电脑通过同一网址访问，数据云端加密同步。

### 1.3 核心理念

- **一个数字最重要**：用户每天打开应用，第一眼要看到"我每天要跑多少钱的单"
- 金额一律显示两位小数
- 口径透明：所有计算都标注"按什么算的"（实际/预计双口径）
- 零成本运行：全免费技术栈，无服务器、无运维

---

## 2. 用户与场景

### 2.1 用户画像

- 京东众包骑手，收入日结进平台钱包（未提现状态）
- 每月有多笔固定还款（按日扣款，部分有最后一期）和偶发的一次性还款
- 手机为主要记录设备（收工后记一笔），电脑为辅助查看设备

### 2.2 典型使用流程

```
每日收工
  → 打开应用「钱包」页，更新京东众包钱包余额（或记一笔手动收入）
  → 「概览」页查看：今日收入、每日需跑单金额、最近账单倒计时
  → 感到缺口大，决定明天多跑 / 提现一部分钱包余额
发薪/还款日
  → 「钱包」页操作提现（填金额+手续费），到账计入总收入
  → 账单扣款后，删除已结清的一次性账单
```

---

## 3. 功能需求

### 3.1 功能地图

```
跑单记账
├── 概览页
│   ├── 统计范围筛选（固定月份 / 自定义日期）
│   ├── 账单总计 & 收入统计卡
│   ├── 京东众包钱包三列卡（钱包余额 / 实时预计总余额 / 可用资金）
│   ├── 最近要还的账单（前两笔重点展示）
│   └── 目标进度双进度条
├── 钱包页
│   ├── 更新钱包余额（弹窗，支持补记日期、同日覆盖）
│   ├── 从钱包提现到总收入（弹窗，含手续费）
│   ├── 余额更新记录列表
│   └── 提现记录列表
├── 记一笔页
│   ├── 收入 / 支出 / 转账 三类型
│   └── 今日记录列表
├── 账单页
│   ├── 账单日历（月视图，含账单信息）
│   ├── 我的账单列表（按扣款日排序）
│   └── 本月账单合计（固定/一次性分组折叠）
├── 历史页（收支明细按月汇总）
└── 同步页
    ├── 云端同步设置（Supabase，推荐）
    └── 手动备份（同步码/备份文件）
```

### 3.2 核心业务规则（口径定义）

以下口径经过多轮用户校准，为**产品核心资产**，修改需谨慎：

#### 3.2.1 资金口径

| 指标 | 定义 |
|---|---|
| 钱包余额 | 京东众包平台未提现金额，取余额记录中最新一条 |
| 可用资金（实际） | 累计已提现收入 − 累计支出（转账不计入收支） |
| 实时预计总余额（预计） | 已提现收入 + 钱包余额 − 累计支出 |
| 今日跑单 | 手动收入 + 当日钱包增量 + 当日提现毛额（排除提现自动生成的收入记录，防止重复计算） |

#### 3.2.2 账单模型

| 字段 | 说明 |
|---|---|
| 类型 | `monthly`（每月固定）/ `once`（一次性） |
| monthly | 每月扣款日（1-31），可选"结束月份"（最后一期，过了即结清） |
| once | 具体扣款日期，过期后归档置灰 |
| 月度计入规则 | monthly 每月计入（结束月当月仍计入，之后不计）；once 仅扣款当月计入 |

#### 3.2.3 缺口计算（累计叠加口径）

**禁止单笔独立与资金比较**。账单按扣款日从近到远排序后：

```
第 N 笔账单的缺口 = 前 N 笔待还账单金额之和 − 资金
日均需跑单       = 缺口 ÷ 剩余天数（含今天）
```

双口径同时展示：

- **实际缺口** = 累计待还 − 可用资金
- **预计缺口** = 累计待还 − 实时预计总余额（含钱包未提现）

每笔账单展示时**先突出本笔应还金额**，累计值仅作为"本笔 + 此前待还"的辅助说明（避免误读为账单被合并）。

#### 3.2.4 提现模型

```
提现金额 W，手续费 F（可为 0），实际到账 A = W − F
效果：
  1. 生成提现记录（W、F、A、备注）
  2. 生成一条收入记录（金额 A，带 wd 标记防重复计入跑单走势）
  3. 钱包余额自动扣减 W，生成一条"提现自动扣减"余额记录
删除提现记录时：连带撤销上述 2、3 的关联数据
```

### 3.3 功能详述

#### 概览页（默认首页）

| 模块 | 需求 |
|---|---|
| 统计范围 | 两种模式：固定月份（月选择器）/ 自定义日期（起止日期）。切换后账单总计、收入、缺口、日均、进度条全部联动；固定账单在跨月范围内按扣款日逐月计次 |
| 账单总计卡 | 显示所选范围的账单总额，副标题显示缺口/超额及日均 |
| 收入卡 | 所选范围的收入合计，副标题显示今日跑单与今日支出 |
| 钱包三列卡 | 钱包余额（含更新时间与今日增量）/ 实时预计总余额 / 可用资金 |
| 最近要还的账单 | 优先展示**未备好**的前两笔（倒计时方块、本笔应还、累计构成、双口径缺口、日均需跑单高亮金额）；已备好的弱化为底部一行汇总；今天扣款未备齐红色强提醒 |
| 目标进度 | 双进度条：实际进度（期间已提现收入/账单 X，蓝色）、预计进度（含钱包未提现，橙色），≥100% 变绿 |

#### 钱包页

| 模块 | 需求 |
|---|---|
| 操作入口 | 两按钮：更新钱包余额 / 从钱包提现，点击弹窗填表（应用内弹窗，非浏览器原生） |
| 更新余额 | 日期（默认今天，可选历史日期补记）+ 余额；同日重复录入询问覆盖；保存时提示所选日期之前的最近余额供核对 |
| 提现 | 金额 + 手续费（≥0）+ 备注；实时预览"实际到账、钱包余额变化"；超额提现二次确认 |
| 余额记录 | 倒序列表：日期、余额、较上次增减；提现自动扣减的带标签；可删除（应用内确认弹窗） |
| 提现记录 | 倒序列表：金额、手续费、备注、到账额；可删除（连带撤销） |

#### 记一笔页

| 模块 | 需求 |
|---|---|
| 类型切换 | 收入（绿）/ 支出（红）/ 转账（蓝）三选一，切换表单字段 |
| 表单 | 日期 + 金额 + 备注；转账额外填转出/转入账户（如 微信→招联金融），**转账不计入收支统计** |
| 今日记录 | 列出今天所有记录，可删除 |

#### 账单页

| 模块 | 需求 |
|---|---|
| 账单日历 | 月视图；日期格内显示账单名称+金额+类型（每格≤2条，超出显示+N笔）；今天蓝色描边；月份前后切换；有账单的日期点击弹详情 |
| 添加账单 | 日历卡片右上角按钮，弹窗表单：名称、金额、类型（每月固定/一次性）、扣款日或扣款日期、结束月份（固定账单选填） |
| 日历弹窗 | 单账单直接显示详情（类型、金额、下次扣款、双口径缺口）+ 关闭/删除/修改按钮；多账单先弹列表选择，并提供"在这天添加"快捷入口（自动带日期） |
| 我的账单 | 按下次扣款从近到远排序；每笔带 固定/一次性 标签、还差标签、扣款信息、结束月信息、双口径日均跑单（金额高亮）；今天扣款红字；已过期一次性/已结清固定的置灰归档 |
| 本月账单合计 | 总计 + 固定账单/一次性账单两个可折叠分组，展开显示各自明细 |

#### 历史页

- 收支明细按月分组汇总（月收入/支出/结余），月内逐条列出（类型标签、转账显示"账户A → 账户B"、备注），可删除

#### 同步页

| 模块 | 需求 |
|---|---|
| 云端同步（推荐） | 填 Supabase Project URL、anon key、同步密码（≥4位，两端一致）；启用/停用开关；立即同步按钮；状态行实时显示同步结果 |
| 自动同步 | 保存后 1.5s 防抖推送；页面回到前台/切到同步页/每 30 秒自动拉取；按时间戳 LWW（后写胜出） |
| 手动备份 | 复制同步码 / 下载 JSON 备份文件；粘贴或选文件导入；支持合并导入（按 id 去重）与覆盖导入 |

### 3.4 非功能需求

| 类别 | 需求 |
|---|---|
| 隐私 | 数据经浏览器端加密（PBKDF2 派生密钥 + AES-GCM）后才上传，云端只见密文；行 ID 由密码哈希派生 |
| 可用性 | 云端异常时自动降级本地模式，不影响记账；无网时可用本地数据 |
| 性能 | 单文件 < 100KB，无外部依赖，首屏秒开 |
| 兼容性 | 现代手机/电脑浏览器（需支持 Web Crypto API 才能启用云端同步） |
| 数据迁移 | 旧版本 localStorage 数据自动迁移（v1→v2→v3）；换设备靠云端同步或备份文件 |
| 成本 | 全免费：静态托管（QW Pages）+ Supabase 免费档（500MB/500万请求每月，远超用量） |
| 版本可辨识 | 页脚显示版本号（如 V23），便于排查浏览器缓存旧版问题 |

---

## 4. 技术架构

### 4.1 技术栈

- **前端**：原生 HTML/CSS/JavaScript（ES5 风格），单文件 `index.html`（约 2000 行），无任何框架与外部依赖
- **存储**：
  - 本地：浏览器 localStorage（key: `paodan_data_v3`）
  - 云端：Supabase（PostgreSQL，PostgREST 接口，前端 `fetch` 直连）
- **加密**：Web Crypto API（PBKDF2 100k 迭代 + AES-GCM-256）
- **部署**：QW Pages 静态托管，单文件发布

### 4.2 数据模型

**本地（localStorage，JSON 整包）**

```json
{
  "bills":     [{ id, name, amount, type: "monthly"|"once",
                  day_of_month?, due_date?, end_month?, created_at }],
  "records":   [{ id, type: "income"|"expense"|"transfer",
                  date, amount, note, from?, to?, wd?, created_at }],
  "walletLogs":[{ id, date, balance, auto?, created_at }],
  "withdraws": [{ id, date, amount, fee, actual, note, created_at }],
  "_ts": "最后修改时间戳（LWW 依据）"
}
```

**云端（Supabase 单表 app_data，单行整包）**

```sql
create table app_data (
  id text primary key,      -- SHA-256(密码)，同密码=同行
  payload text not null,    -- 加密后的整包 JSON（同上结构 + updated_at）
  updated_at timestamptz
);
```

设计权衡：选择"单行整包 + 端侧加密"而非"每条一行"——换取零后端、零改造、隐私可控、备份即快照；代价是整包上传不增量，但单用户数据量（月均几百条，几十 KB）完全在免费额度内。

### 4.3 同步机制

```
保存数据 ──┬─→ localStorage（立即）
           └─→ 防抖 1.5s ─→ 加密整包 ─→ POST upsert 云端

拉取时机：页面回到前台 / 切到同步页 / 每 30 秒定时
拉取逻辑：取云端行 → 解密 → 比对 _ts 时间戳
           云端新 → 覆盖本机并刷新界面
           本机新 → 自动推送
           相等   → 无操作
```

---

## 5. 版本迭代记录（摘要）

| 版本 | 主要变更 |
|---|---|
| V1-V4 | 基础版：账单倒计时、每日目标、收支记录、京东钱包（余额/提现） |
| V5-V8 | 钱包日期补记、双口径、双进度条 |
| V9-V10 | 应用内弹窗改造、账单日历、类型标签、两笔重点展示 |
| V11-V13 | 累计叠加缺口口径（用户纠偏）、两位小数、本笔优先展示 |
| V14-V19 | 日历增强（弹窗改删、格内账单信息）、合计分组折叠、钱包页弹窗化 |
| V20 | 概览统计范围筛选（月/自定义）、固定账单结束月份 |
| V21 | 云端同步（Supabase + 端侧加密，双模式可切换） |
| V22 | 30 秒定时拉取（修复前台页面不更新问题） |
| V23 | 日均跑单金额高亮样式 |

## 6. 已知限制与风险

| 项 | 说明 | 缓解措施 |
|---|---|---|
| 并发冲突 | 两端 30 秒内同时改动，后保存者覆盖前者（LWW 整包） | 使用习惯上错开；后续可考虑操作日志合并 |
| Supabase 休眠 | 免费档 7 天无访问自动休眠 | 每日使用即无感；休眠后 Restore 激活，数据不丢 |
| 密码丢失 | 同步密码丢失则云端数据无法解密 | 手动备份兜底；密码可自定义备份保存 |
| 单文件规模 | 代码持续增长（约 2000 行），无模块化 | 规模再扩大时重构为模块化/引入轻量框架 |
| 平台依赖 | 静态托管依赖 QW Pages | 单 HTML 文件可随时迁移至 GitHub Pages/Vercel 等 |

## 7. 后续规划（Backlog，未排期）

- [ ] 记一笔/历史/钱包删除操作统一为应用内确认弹窗（部分仍为原生 confirm）
- [ ] 账单扣款日当天自动生成提醒（需引入推送或日历订阅，评估免费方案）
- [ ] 多账期结清模拟器（"如果我每天跑 X 元，哪天能全部还完"）
- [ ] 年度统计视图（月度收入/支出对比图表）
- [ ] 数据导出为 Excel/CSV

---

*本文档由开发过程整理而成，功能口径以最新线上版本为准。*
